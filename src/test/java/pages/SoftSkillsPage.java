package pages;

import org.openqa.selenium.WebDriver;

public class SoftSkillsPage {
    public HeaderPage header;
    WebDriver driver;
    public SoftSkillsPage(WebDriver driver) {
        this.driver = driver;
    }
}
